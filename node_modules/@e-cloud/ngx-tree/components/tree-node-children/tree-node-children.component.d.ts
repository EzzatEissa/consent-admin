import { OnChanges, OnDestroy, OnInit, SimpleChanges } from '@angular/core';
import { TreeNode, TreeUIOptions } from '../../models';
import { TreeVirtualScroll } from '../../services/tree-virtual-scroll.service';
/** Time and timing curve for expansion panel animations. */
export declare const EXPANSION_PANEL_ANIMATION_TIMING = "225ms cubic-bezier(0.4,0.0,0.2,1)";
export declare class TreeNodeChildrenComponent implements OnInit, OnChanges, OnDestroy {
    private virtualScroll;
    marginTop: number;
    viewportNodes: TreeNode[];
    options: TreeUIOptions;
    node: TreeNode;
    templates: any;
    disableMarginTop: boolean;
    children: TreeNode[];
    refreshTree: boolean;
    expandAnimation: boolean;
    className: boolean;
    private scrollSub;
    constructor(virtualScroll: TreeVirtualScroll);
    readonly noPadding: boolean;
    readonly marginTopAttr: number;
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngOnDestroy(): void;
    trackNode(index: any, node: any): any;
    calcMarginTop(): number;
    getViewportNodes(nodes: TreeNode[], {startPos, endPos}: {
        startPos: any;
        endPos: any;
    }): TreeNode[];
}
