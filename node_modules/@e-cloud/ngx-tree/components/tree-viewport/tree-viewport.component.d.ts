import { AfterViewInit, ElementRef, OnChanges, OnDestroy, OnInit, Renderer2, SimpleChanges } from '@angular/core';
import { ScrollIntoViewTarget, TreeModel } from '../../models';
import { TreeVirtualScroll } from '../../services/tree-virtual-scroll.service';
export declare class TreeViewportComponent implements OnInit, OnChanges, AfterViewInit, OnDestroy {
    virtualScroll: TreeVirtualScroll;
    private elementRef;
    private renderer;
    lastScrollTop: number;
    isScrolling: boolean;
    enable: boolean;
    treeModel: TreeModel;
    className: boolean;
    private ticking;
    private scrollIntoViewTicking;
    private lastScrollIntoViewTarget;
    private structureChangeSub;
    private scrollIntoViewSub;
    private scrollTimer;
    constructor(virtualScroll: TreeVirtualScroll, elementRef: ElementRef, renderer: Renderer2);
    onScroll(event: any): boolean;
    ngOnInit(): void;
    ngOnChanges(changes: SimpleChanges): void;
    ngAfterViewInit(): void;
    ngOnDestroy(): void;
    scrollIntoViewAndTick(target: ScrollIntoViewTarget, scrollCallback: Function): void;
    initEventSubscription(): void;
    setViewport(): void;
    private disableEventsWhenScrolling();
}
