import { InjectionToken } from '@angular/core';
import { Subscription } from 'rxjs';
import { TreeModel, TreeNode } from '../models';
export declare const VIRTUAL_SCROLL_NODE_HEIGHT_QUOTA: InjectionToken<{}>;
export declare class TreeVirtualScroll {
    private quota;
    id: number;
    averageNodeHeight: number;
    hasEnoughNodeHeight: boolean;
    private currentViewport;
    private lastScrollTop;
    private disabled;
    private collectionMonitor$;
    private nodeHeightAnalytics$;
    constructor(quota: number);
    adjustViewport(viewport: ClientRect, scrollTop: number): void;
    waitForCollection(observer: any): Subscription;
    reportNodeHeight(data: any): void;
    reCalcPositions(treeModel: TreeModel): void;
    setDisabled(isDisabled: any): void;
    isDisabled(): boolean;
    scrollIntoView(node: TreeNode, force: boolean, scrollToMiddle?: boolean): number;
    private getPositionAfter(nodes, startPos);
    private getPositionAfterNode(node, startPos, isPrevShadow?);
    private collectAverageNodeHeight();
}
